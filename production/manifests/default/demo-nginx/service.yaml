# kubernetes/nginx/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    version: "v9"
spec:
  selector:
    app: nginx
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP
